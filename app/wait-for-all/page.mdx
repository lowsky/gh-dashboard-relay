import { LinkList } from 'components/LinkList';

## Wait for all data are loaded.

Embedded into one React `<Suspense/>` with one Global Spinner as a `fallback`.

<LinkList rootPath="/wait-for-all" />
