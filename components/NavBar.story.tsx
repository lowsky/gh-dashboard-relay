import React from 'react';

import { NavBar } from './NavBar';

export default {
    title: 'Others/NavBar',
    component: NavBar,
};

export const Default = (props) => <NavBar {...props} />;

Default.args = {};
