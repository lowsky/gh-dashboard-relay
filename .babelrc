{
    // only needed for starting the graphql proxy server, and
    // running jest per react-app-rewired
    "plugins": [
        "relay"
    ],
    "presets": [
        //"react",
        "es2015",
        "stage-3"
    ]
}
